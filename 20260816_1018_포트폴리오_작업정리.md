# 포트폴리오 작업 정리

> 심민용 포트폴리오 (`minyongsim/portfolio_sim2026`)
> 정리 시점: 2026년 8월 16일 10:18 KST
> 이전 정리: `20260814_1609_포트폴리오_작업정리.md`

---

## 이번 작업의 큰 줄기

세 가지입니다.

1. **BRAND STORAGE 페이지 정리** — 사진을 덜어내고, 소개 문장 아홉 개를 전부 2줄로 맞추고, 성격에 안 맞던 숫자 블록 두 개를 손봤습니다. 유실됐던 **패키지 리뉴얼 섹션도 깃 히스토리에서 되살렸습니다.**
2. **이름과 로고 교체** — `BRAND STORAGE` → **BRANDING**, 이미지 로고 → **글자 로고(PORTFOLIO)**.
3. **웹 이력서 신설** — `resume.html`. 사람인 내보내기 PDF를 사이트에 거는 대신, 사이트 톤으로 이력서 페이지를 만들고 브라우저 인쇄로 PDF가 나오게 했습니다.

그 과정에서 **CSS 버그 세 개와 공개 노출 위험 하나**를 발견해 같이 처리했습니다.

---

## 1. BRANDING 페이지 (구 BRAND STORAGE)

### 사진 정리

| 섹션 | 이전 | 지금 | 뺀 것 |
|---|---|---|---|
| 01 브랜드 정체성 | 12장 | **8장** | 발리 탐바칸 블렌드, 코피 발리 박스, 토라자 골드 박스, 원두 자 연출컷 |
| 03 마케팅 실행 | 12장 | **8장** | 탐바칸 워싱, 풀워시드 가공, 현장 시음, 인스타 '꼭 사야 할 커피' |

둘 다 4×2로 떨어집니다. 뺀 이미지 파일은 `img/`에 그대로 남아 있습니다.

### 패키지 리뉴얼 섹션 복구

8월 14일에 `brand-story.html`이 예전 버전으로 덮이면서 통째로 사라졌던 섹션입니다. 깃 히스토리 **`83eb53b`** 에서 원본을 찾아 되살렸습니다.

- 위치: **01 브랜드 정체성 바로 아래, 02번**
- 옛 버전은 `브랜드 라벨` 형식이었는데 지금 페이지 규칙인 `02 ─ 패키지 리뉴얼` 머리표로 교체
- 미사용이던 `sinsan-card-*.jpg` 4장이 드디어 쓰였습니다 (`.isCard` 스타일이 아직 살아 있어 그대로 붙음)

이 삽입으로 신산커피 섹션 번호가 밀렸습니다 — 마케팅 실행 03 / 데이터 검증 04 / 결과 05.

### 소개 문장 아홉 개를 전부 2줄로

PC(1280px) 기준으로 신산커피 5개 + 메르디센트 4개 모두 정확히 2줄입니다. 줄인 곳은 여섯 개입니다.

| 섹션 | 이전 | 지금 |
|---|---|---|
| 03 마케팅 실행 | 4줄 | 2줄 |
| 04 데이터 검증 | 3줄 | 2줄 |
| 05 결과 | 3줄 | 2줄 |
| 메르디센트 01 기획 | 3줄 | 2줄 |
| 메르디센트 02 구축 | 3줄 | 2줄 |
| 메르디센트 04 결과 | 3줄 | 2줄 |

**줄이면서 아래 흐름도·태그·수치와 겹치던 말을 뺐습니다.** 예를 들어 03은 "농장 영상 상영 / 아라비카 시음 / 다구매 프로모션"이 바로 밑 흐름도에 아이콘으로 또 나오고 있어서, 문장은 *왜 그렇게 했는지*만 남겼습니다. 04도 `7~8%` 수치 블록이 옆에 있어 설명을 반으로 줄였습니다.

모바일(390px)은 아홉 개 모두 3줄로 일정합니다.

### 01 섹션 소개 문장 — 희소성의 주체를 바로잡음

기존 문장은 *"…말이 아니라 제품으로 보여줬습니다"* 로 끝나면서, **희소성의 공을 원두가 아니라 패키지 쪽으로 넘기고** 있었습니다. 희소한 건 발리 아라비카라는 원두입니다. 게다가 패키지 이야기는 바로 아래 02 섹션이 통째로 맡고 있어 미리 꺼낼 이유도 없었습니다.

다섯 가지 대안을 만들어 검토한 뒤 채택한 문장:

> 고객이 신산커피를 골라야 할 이유가 필요했습니다.
> 그 이유를 브랜드 크기가 아니라, 여기서만 만날 수 있는 원두에서 찾았습니다.

역할이 나뉩니다 — 헤드라인은 **사실**(유일했다), 소개 문장은 **판단**(그래서 원두에서 이유를 찾았다), 흐름도는 **순서**, 02 섹션이 **실행**.

### 메르디센트 02 구축 — 큰 숫자 "4" 제거

`4` + "기획·디자인·배송·결제, 단독 담당 영역"이 큰 숫자로 서 있었습니다. 두 가지가 어긋났습니다.

- **내용** — 4는 성과가 아니라 담당 영역 **개수**입니다. 바로 위 흐름도가 이미 그 4개를 아이콘으로 보여주고 있어, 숫자가 하는 일이 "위에 있는 걸 세어보세요"뿐이었습니다. 같은 페이지의 `7~8%`(실구매 전환율), `TOP 10`(주최 측 선정)과 같은 크기로 놓이면 4가 성과처럼 보여 나머지 두 숫자의 무게까지 떨어뜨립니다.
- **디자인** — 사진 6장 띠 → 캡션 → 또 큰 숫자로 섹션이 세 번 끝났고, 4열 그리드 안에 stat 하나만 들어 있어 혼자 떠 있었습니다.

**사실은 문장에 넣어 살렸습니다** — "…결제 연동 **네 가지**를 순서대로 세웠습니다." 헤드라인("전 과정을 혼자 세팅했습니다")과 똑같이 끝나던 중복도 같이 정리됐습니다.

### 03 마케팅 실행 — TOP 10을 배지로

형태가 성격과 안 맞았습니다. `7~8%`는 **측정한 수치**, `TOP 10`은 **주최 측이 매겨준 결과**인데 둘 다 64px 코랄 숫자로 서 있으니 TOP 10이 지표처럼 읽혔습니다.

같은 페이지 **주문 흐름 카드와 똑같은 규칙**(1.5px `#E8E8E8` 테두리, 라운드, `#FFF1EF` 원 안의 코랄 아이콘)으로 배지를 만들었습니다. 새 스타일이 아니라 기존 언어의 재사용입니다.

- `fa-award` 리본 아이콘 — "받은 것"이라는 신호
- 숫자 64 → **30px**, 아이콘 옆에 라벨과 세로로 묶어 한 덩어리로
- hover 시 테두리 코랄 + 3px 부상

이제 페이지에서 제일 큰 글자는 실제로 측정한 지표 하나(`7~8%`)뿐입니다.

> `fa-award`가 이 사이트가 쓰는 **FA 6.0.0 free에 실제로 있는지 패키지를 받아 확인**했습니다(`\f559`). 예전에 `fa-mobile-screen`이 빈칸으로 나왔던 것과 같은 사고를 막기 위해서입니다. 같은 검사에서 `fa-people-group`은 6.0.0에 **없어서** `fa-handshake`로 교체했습니다.

---

## 2. 줄바꿈 전수 검토 — CSS 버그 발견

PC/모바일 양쪽에서 모든 본문 요소의 줄바꿈 지점을 뽑아 검토했습니다. 그러다 **`word-break: keep-all`이 두 곳에서 빠져 있는 것**을 찾았습니다. 헤드라인·본문에는 이미 걸려 있던 규칙인데 `.aboutBridge`와 `.brandIntro p`만 누락돼서, 390px에서 한글이 **글자 단위로 쪼개지고** 있었습니다.

| 위치 | 이전 | 지금 |
|---|---|---|
| 인트로 문장 | 브랜드 ⏎ **를** | 브랜드를 ⏎ 처음부터 |
| 패키지 리뉴얼 캡션 | 자 ⏎ **기** | 보고 ⏎ 자기 |
| 결과 캡션 | 브 ⏎ **랜드가** | 건 ⏎ 브랜드가 |
| 구축 캡션 | 상 ⏎ **세,** | 상세, ⏎ 배송·교환환불 |
| 운영 캡션 | 않 ⏎ **기** | 않기 ⏎ 때문에, |

CSS 한 줄로 다섯 군데가 한 번에 잡혔습니다.

### 가운뎃점(·) 처리

`·`는 어절 경계가 아닌데도 줄바꿈 지점이 됩니다. `keep-all`로는 막히지 않아서 `.nbGroup { white-space: nowrap }`을 만들어 묶었습니다.

- `노출· ⏎ 결제·배송` (운영 섹션)
- `영문 상세 · ⏎ 옵션` 등 주문 흐름 카드 3개

### 어절 묶음(`&nbsp;`)

`패키지 리뉴얼을`, `감사 프로모션 참여`, `알 수 있게`, `수요가 있었습니다` 등을 묶었습니다.

> 반대로 **묶었다가 도로 푼 곳도 있습니다.** 01 섹션에서 문장 단위로 줄을 맞추려고 `그 이유를 브랜드 크기가`를 묶었더니 오히려 1줄이 길어지면서 `크기가 / 아니라`가 쪼개졌습니다. 그냥 두는 쪽이 어절이 온전했습니다.

**최종 검사: 어절 중간 분리 0건** (1280·390 양쪽).

---

## 3. ABOUT 04번 섹션 문구

> ~~숫자 뒤에 어떤 기획이 있었는지, 아래에서 확인하세요.~~
> **만들고, 팔고, 확인한 과정을 세 갈래로 나눠 담았습니다.**

세 가지가 어긋나 있었습니다.

- **"숫자 뒤에"가 사실과 안 맞음** — 바로 앞 03 섹션 덤벨 차트는 NDA 때문에 수치를 하나도 표기하지 않습니다. 되짚어보면 그 숫자가 없습니다.
- **"아래에서 확인하세요"는 톤이 튐** — 사이트의 모든 문장이 평서형인데 여기만 명령형 안내문이고, 버튼이 바로 밑에 있어 위치 안내도 불필요합니다.
- **헤드라인과 중복** — "증거는 케이스에 있습니다"가 이미 같은 뜻입니다.

바꾼 문장은 첫 화면 헤드라인(`만들고, 팔고, 확인합니다`)을 과거형으로 받아 **수미상관**을 만듭니다.

---

## 4. 로고 교체 — 이미지에서 글자로

`img/logo-script.png`(필기체) → **PORTFOLIO** 글자 로고. 본문과 같은 런드리고딕이라 사이트 전체가 한 벌의 서체로 묶입니다.

- `js/partials.js` 한 곳만 고쳐서 **상단바·푸터·전 페이지 동시 반영**
- 26px / 자간 2px / `font-weight: 700`
- 스크롤 시 26 → 20px (이전 이미지의 42 → 30px 축소를 재현)
- hover 코랄, 푸터는 흰색 반전(24px)

> `.work 2026` 부제를 넣었다가 뺐습니다. 굵기는 800으로 잡았다가 **700으로 명시**했습니다 — `font.css`를 보니 런드리고딕은 R(400)/B(700) 두 벌뿐이라, 800을 요청하면 가짜 볼드가 그려질 수 있습니다.

### 여기서 잡은 버그 둘

**상단바가 111 → 116px로 커졌습니다.** `inline-block`이 글자 베이스라인에 앉으면서 아래에 디센더 여백 약 5px이 붙은 것이었습니다. `vertical-align: top`으로 잡아 **110px**로 되돌렸습니다(스크롤 시 90px 동일). 상단바 높이는 `.chromeSpacer`와 섹션 스냅 기준점의 근거라 안 흔들리는 게 중요합니다.

**푸터 로고가 검은 글자로 나왔습니다.** 색을 `style.css`에 넣었는데 `site.css`가 나중에 로드되면서 같은 명시도의 `.logoText .logoTitle`(#373737)에 밀렸습니다. 푸터 규칙을 `site.css`로 옮겨 해결했습니다 — 어두운 배경이라 안 고쳤으면 아예 안 보였을 겁니다.

---

## 5. BRAND STORAGE → BRANDING

**왜 바꿨나**

- **형제 메뉴와 층위가 안 맞음** — `DATA & CRM`, `MARKETING`은 직능 이름인데 `BRAND STORAGE`만 비유로 만든 조어라 읽는 사람이 한 박자 멈춥니다.
- **이름과 내용이 반대 방향** — STORAGE는 *보관·창고*인데 그 페이지 첫 문장은 "브랜드를 **세우는** 기준"입니다. 영어권에서 storage는 자기창고·디스크 저장이 먼저 떠오릅니다.

`BRANDING`은 세 메뉴를 모두 직능 이름으로 통일하면서 첫 화면 세 동사와 1:1로 맞습니다 — **만들고 = BRANDING · 팔고 = MARKETING · 확인합니다 = DATA & CRM**.

**바꾼 곳 13군데**

| 파일 | 내용 |
|---|---|
| `js/partials.js` | 상단 메뉴 (전 페이지 반영) |
| `js/site.js` | 띠배너 현재 위치 표시 |
| `brand-story.html` | 페이지 타이틀 · OG 타이틀 · 케이스 태그 |
| `about.html` · `index.html` · `404.html` | 하단 버튼 |
| `projects/global-launch.html` | 돌아가기 링크 2곳 · 태그 |
| `css/site.css` | 주석 2곳 |

곁들여 `BRANDING로 돌아가기` → **`BRANDING으로`**로 고쳤습니다. 이전 이름은 "브랜드 스토리지"라 `로`가 맞았지만 "브랜딩"은 받침이 있습니다.

**파일명 `brand-story.html`은 그대로 뒀습니다.** 바꾸면 공유된 링크, `data-page` 키, `projects/` 하위 페이지 연결까지 다 손봐야 하는데 주소는 사람이 보는 이름이 아니라 얻는 게 없습니다.

---

## 6. `index.html`이 `about.html`의 복사본

헤드·메타 5줄만 다르고 본문 4개 섹션이 똑같습니다. 그래서 ABOUT 04번 문구를 고쳤을 때 **홈에는 반영되지 않아** 옛 문구가 남아 있었습니다. 맞췄습니다.

> **앞으로도 ABOUT 본문을 고칠 때마다 `index.html`에 같이 반영해야 합니다.** 계속 손으로 맞추는 게 부담이면 홈을 about.html로 리디렉트하거나 공통 부분을 분리하는 방법이 있습니다.

---

## 7. 웹 이력서 신설 — `resume.html`

### 왜 사람인 PDF를 그대로 안 걸었나

받아본 최신 이력서는 채용사이트 내보내기 양식이었습니다. 공개 URL에 올리면 곤란한 것이 섞여 있습니다.

- **연봉 3,000만원 · 2,020만원 · 퇴사사유 경영악화** — 사람인 안에서는 정상이지만 공개되면 **다음 연봉 협상에서 상대가 하한선을 먼저 알고 시작합니다.**
- **주소 · 생년 · 나이 · 증명사진** — `minyongsim.github.io`는 크롤링되는 공개 사이트입니다.
- 프로필 카드의 **포트폴리오 항목이 `-`** 로 비어 있었습니다.
- 자기소개서 네 꼭지가 사이트 케이스와 **같은 이야기를 훨씬 길게** 반복했습니다.
- 잡보드 기본 템플릿이라 RESUME를 누르는 순간 브랜드가 끊겼습니다.

**용도를 나눴습니다** — 사람인·원티드 지원용은 지금 PDF 유지, 사이트는 별도 웹 이력서.

### 만든 것

| 항목 | 내용 |
|---|---|
| 폰트 | **Pretendard** (이력서 본문만). `css/font.css`에 `@font-face` 9종 선언, 적용은 `.resumeWrap` 안쪽으로 한정 |
| 구성 | 머리말 / 강점 / 핵심 역량 4카드 / 경력 3곳 / 스킬 / 학력·교육 |
| 경력 → 케이스 | 두두·메르디센트에서 각각 `brand-story.html#sinsan`, `#merdescent`로 바로 링크 |
| 강점 | 자기소개서를 옮기지 않고 3~4줄로. 근거는 BRANDING 페이지가 맡음 |
| 사이드 탭·휠 스냅 | **끔.** 이력서는 스크롤 덱이 아니라 문서라서 (`.resumeSection`은 site.js 섹션 셀렉터에 안 걸림) |

**뺀 것** — 연봉, 퇴사사유, 주소, 생년·나이, 증명사진, 희망연봉. 최종 검사 0건 확인.

### PDF

미리 만든 파일 대신 **"PDF로 저장" 버튼(`window.print()`)** 입니다.

- 파일을 두면 본문을 고칠 때마다 다시 내보내야 하고, 안 하면 화면과 PDF가 달라집니다
- 브라우저 인쇄는 항상 최신이고, **사용자 브라우저의 웹폰트로 렌더링돼 화면과 똑같이** 나옵니다

인쇄용 CSS(`@media print`)를 따로 짰습니다 — 띠배너·상단바·푸터·버튼 제거, 업무 그룹이 페이지 경계에서 안 잘리게, 회사명만 페이지 끝에 남는 것 방지, 사이트 주소는 머리말에 한 번만. A4 기준 3~4페이지입니다.

### 총 경력 자동 계산

재직 중인 회사가 있으면 "4년 8개월"은 다음 달이면 틀립니다. 끝난 경력의 합(26개월)에 2024.03부터 오늘까지를 더해 화면에서 다시 적도록 했습니다.

| 열어보는 날 | 표시 |
|---|---|
| 2026.08 | 총 4년 8개월 |
| 2026.12 | 총 5년 |
| 2027.03 | 총 5년 3개월 |

개월이 0이면 "5년 0개월" 대신 "5년"만 적습니다. JS가 안 돌면 HTML에 적힌 값이 그대로 보입니다. 두두 항목은 개월 수 대신 **"재직 중"** 으로 표시합니다.

### 사진은 넣지 않기로

공개 사이트에 증명사진을 올리면 (1) 색인·캐시가 남아 되돌릴 수 없고, (2) 첫 화면에서 "데이터로 검증하는 마케터"라는 문장보다 얼굴이 먼저 읽히고, (3) 해외·외국계는 사진 있는 이력서를 리뷰에서 제외하기도 합니다. 사람인 지원서에는 이미 사진이 들어가므로 실익 손해도 거의 없습니다.

---

## 8. ⚠️ 공개 노출 — 확인 필요

**기존 `pdf/simminyongresume.pdf`(사람인 내보내기)는 이미 커밋·푸시돼 공개 상태였습니다.** `_to_delete/`로 옮겼으니 다음 푸시하면 사이트에서는 사라집니다.

**다만 깃 히스토리에는 남아 있어** 이전 커밋을 뒤지면 접근 가능합니다. 연봉·주소가 걱정되시면 히스토리 정리(`git filter-repo`)나 저장소를 새로 만드는 것을 검토해야 합니다.

---

## 9. 그 밖에

- **`.git/index.lock`** — 상태 확인용 git 실행이 락 파일을 남겼습니다(이 환경에선 파일 삭제가 막혀 있음). `_to_delete/`로 치웠습니다.
- **`_to_delete/` 폴더는 통째로 지우셔도 됩니다.** 커밋에 안 들어가게 `.gitignore`에 넣어두는 것도 좋습니다.
- **워터마크 중복 확인** — 이전 정리의 숙제였는데, 03 섹션 배경 `GROWTH`가 다른 태그와 겹치지 않는 것을 확인했습니다. **해결.**
- **깃 상태** — 8월 14일 21:41 `이걸로 브랜딩 까지 완료` 커밋·푸시가 되어 있습니다.

---

## 남은 일

| 항목 | 내용 |
|---|---|
| **빈 페이지** | `data-crm.html` · `marketing.html`이 아직 2.6KB 껍데기. 메뉴와 하단 버튼이 전부 이쪽을 가리킴 |
| **하단 버튼 아이콘** | 폴더(`fa-folder-open`)가 "저장소"일 땐 맞았지만 BRANDING에는 안 맞음. `fa-swatchbook`·`fa-pen-nib` 등 후보 |
| **이메일 통일** | 사이트·이력서는 `eq900bmwm@gmail.com`, 사람인 PDF는 `alsdyd5411@naver.com` |
| **깃허브 업로드** | 이번 변경분 + `resume.html` 신규. `_to_delete/`는 빼고 올릴 것 |
| **깃 히스토리** | 사람인 PDF가 과거 커밋에 남아 있음 (위 8번) |
| **메르디센트 로고 원본** | 캡처에서 추출한 것이라 작게만 사용 가능 |
| **8% 문구 통일** | ABOUT·홈은 "신규 고객 재구매 전환율", BRANDING은 "신규 유입 팔로워 실구매 전환율" — 같은 수치인지 확인 필요 |

---

## 파일 현황

| 파일 | 크기 | 비고 |
|---|---|---|
| `index.html` | 9,592 B | about.html 복사본 (메타 5줄만 다름) |
| `about.html` | 9,611 B | |
| `brand-story.html` | 28,355 B | 신산커피 5섹션 + 메르디센트 4섹션 |
| `resume.html` | — | **신규** |
| `data-crm.html` | 2,675 B | 미완성 |
| `marketing.html` | 2,653 B | 미완성 |
| `404.html` | 1,763 B | |
| `css/site.css` | — | 이력서·배지·로고 규칙 추가 |
| `css/style.css` | — | 푸터 로고 규칙을 site.css로 이관 |
| `css/rwd.css` | — | 이력서 모바일 규칙 추가 |
| `css/font.css` | — | Pretendard 9종 추가 |
| `js/site.js` | — | RESUME 라벨, 인쇄 버튼, 경력 자동 계산 |
| `js/partials.js` | — | 글자 로고, BRANDING, RESUME 링크 |

---

## 검증 방식

모든 변경은 실제 브라우저(Chromium)로 렌더링해 확인했습니다.

- **PC(1280px) / 모바일(390px)** 양쪽 — JS 에러 0건, 가로 스크롤 넘침 0건
- 소개 문장은 **글자 단위로 좌표를 재어 줄 수와 줄바꿈 지점을 실측** — 어절 중간 분리 0건
- 아이콘은 **Font Awesome 6.0.0 패키지를 직접 받아 존재 여부를 확인** (`fa-people-group` 부재를 이 방법으로 발견)
- 인쇄 CSS는 실제로 PDF를 출력해 페이지별로 확인
- 총 경력 자동 계산은 **시각을 2026.12 / 2027.03으로 바꿔가며** 검증
- 상단바 높이는 변경 전후를 각각 측정해 비교(111 → 110px)

> **한 가지 한계** — 이 작업 환경에서 런드리고딕 웹폰트 CDN이 막혀 Noto Sans CJK로 대체돼 측정됐습니다. 한글이 꽉 찬 폭이라 실제 폰트보다 넓은 쪽이고, 2줄로 맞춘 아홉 문장은 **2줄 용량의 63~83%만 쓰고 있어** 폰트가 20% 넓어져도 2줄을 유지합니다. 그래도 실제 브라우저에서 한 번 확인해보시는 게 확실합니다.
