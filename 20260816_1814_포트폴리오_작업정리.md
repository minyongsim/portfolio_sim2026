# 포트폴리오 작업 정리

> 심민용 포트폴리오 (`minyongsim/portfolio_sim2026`)
> 정리 시점: 2026년 8월 16일 18:14 KST
> 이전 정리: `20260816_1018_포트폴리오_작업정리.md`

---

## 이번 작업의 큰 줄기

**빈 껍데기였던 `data-crm.html`과 `marketing.html`을 채웠습니다.** 그 과정에서 사이트의 세 갈래(BRANDING · MARKETING · DATA & CRM)가 서로 무슨 얘기를 맡을지가 정리됐고, 케이스 상세 페이지 세 개를 덜어냈습니다.

1. **DATA & CRM 신설** — 5섹션 문서형. 논지는 *"없는 걸 만들기 전에, 있는 걸 다시 썼습니다"*
2. **MARKETING 신설** — 4섹션. 케이스 나열이 아니라 **역량 페이지**로. 논지는 *"예산으로 못 이기면, 실행으로 이깁니다"*
3. **케이스 3개 제거** — BJ 공동구매 · 글로벌 런칭 · 박람회

그 과정에서 **CSS 버그 네 개와 사진 처리 사고 두 건**을 발견해 같이 처리했습니다.

---

## 1. DATA & CRM (`data-crm.html`)

2.6KB 껍데기 → **25KB · 5섹션**

| 섹션 | 문제 | 한 일 |
|---|---|---|
| 01 축 바꾸기 | 한 제품에만 쏠림 | 채널을 늘리는 대신 매달 보여줄 향 노트를 바꾸기로 |
| 02 명단과 메시지 | 고객이 네 군데로 흩어짐 | 한 명단으로 통합 + 무드 축 메시지 |
| 03 실행 | — | 온·오프라인 동시 + 할인 설계 + 월간 사이클 |
| 04 결과 | — | 관심 분산 · 빗나간 예측과 그 이유 |
| 05 다른 브랜드 | 커피에서도 같은 문제 | 감사 프로모션을 검증 장치로 겸용 |

### 형식을 문서형으로

처음엔 ABOUT과 같은 스크롤 덱(`.scrollTestSection` t1~t4)으로 만들었는데, **공통 부품이 12개나 되어 ABOUT의 확대판처럼 읽혔습니다.** BRANDING이 쓰는 문서 문법(`.brandStorySection` + `.brandSubSection`)으로 갈아엎어 공통 부품을 **12개 → 7개**로 줄였습니다.

- 탭은 쓰지 않고 레이아웃 문법만 빌림
- 휠 스냅은 끔 — 섹션 높이가 제각각인 문서에선 방해가 됨 (BRANDING에서 이미 같은 이유로 뺐던 판단)
- `js/site.js`에 `data-crm` · `marketing`도 `.brandSubSection`을 섹션으로 세도록 추가

### 신산커피 섹션을 맨 뒤로

원래 01이 신산커피, 02~05가 메르디센트였습니다. 시작하자마자 브랜드가 바뀌어 흐름이 끊기고 곁다리로 읽혔습니다. 맨 뒤로 보내면서 성격을 바꿨습니다.

> `05 ─ 커피에서도 같은 문제가 있었습니다`
> **카테고리가 바뀌어도, 푸는 순서는 같았습니다**

사례가 하나 더 있는 게 아니라 **방법이 브랜드를 안 가린다는 증명**이 됐습니다.

### 홈의 덤벨 차트를 여기로 이관

`about.html` · `index.html` 03 섹션이 카테고리 분산 결과를 차트로 다 보여주고 있어서, 이 페이지의 절반이 재방송이었습니다. 차트는 DATA & CRM으로 옮기고 홈에는 **3단계 흐름도(쏠림 → 매달 향 노트 → 분산) + 링크**만 남겼습니다.

### 카피 — 지어내지 않고 인용

실제 인쇄물에 있던 문장을 인용하고 출처를 밝혔습니다.

> **그달의 향을 알릴 때** — 이번 달, 새로운 향을 발견하는 시간.
> **두 번째 제품을 권할 때** — 낮의 무드와 밤의 무드는 다릅니다. 하나는 곁에 두고, 하나는 잠들기 전 당신의 살결에 남겨보세요.
> *위는 대표 이미지와 매장 전단에 실린 문구, 아래는 같은 기준으로 다시 옮긴 메시지입니다.*

아래 문장은 실제 발송본이 아니라 브랜드 어법(상세페이지의 `당신의` · `~보세요` · `잠들기 전`)으로 다시 쓴 것이라, **면접에서 "실제로 쓰신 건가요"를 받아도 걸리지 않도록 출처를 한 줄로 밝혔습니다.**

### 논지를 두 번 고쳤습니다

처음엔 *"할인율을 앞에 세우지 않았습니다"*라고 썼는데, **대표 이미지에 `SPECIAL BENEFIT · UP TO 20% OFF`가 크게 박혀 있어 자료와 모순**이었습니다. 다음 구조로 다시 썼습니다.

> 메인 장치는 할인이었습니다. 다만 할인만 앞세우면 어느 브랜드나 하는 세일이 됩니다.
> 메르디센트는 **충성 고객층이 두터워** CRM 전환율이 이미 높았습니다. 브랜드의 말이 통하는 조건이 갖춰져 있다는 뜻이라, 끌어당기는 문장을 **브랜드의 언어로 옮겼습니다.**
> **할인이 조건을 만들고, 브랜드의 말이 이유를 만드는 구조**였습니다.

### 운영기간 표기 제거 (11곳)

캠페인은 중간에 멈춘 적 없이 매달 이어졌는데 `6개월`이 **기간**으로 읽히고 있었습니다.

| 위치 | 이전 | 지금 |
|---|---|---|
| 케이스 메타 | `기간 · 6개월 운영` | `주기 · 매달 · 상시 운영` |
| 케이스 스탯 | `6개월 / 캠페인 운영 기간` | `월 1회 / 향 노트 교체 주기` |
| 홈 · ABOUT | `6개월간의 향 노트 캠페인` | `매달 이어간 향 노트 캠페인` |

차트 범례만 `6개월 뒤`로 남기고(운영 기간이 아니라 **비교 시점**), 각주에 못박았습니다 — *"캠페인은 중간에 멈춘 적 없이 매달 이어졌고, 시작 시점과 6개월 뒤를 비교한 것입니다."*

---

## 2. MARKETING (`marketing.html`)

2.6KB 껍데기 → **16KB · 4섹션**

> **예산으로 못 이기면, 실행으로 이깁니다**
> `담당 범위` → `기준` → `검색` → `만드는 비용`

### 세 번 갈아엎었습니다

처음엔 박람회 케이스로 채웠는데, **박람회는 BRANDING(정체성 체감)과 DATA & CRM(구매 검증)이 이미 각각 다루고 있어** 세 페이지가 같은 사건을 세 번 말하게 됐습니다. 박람회를 전부 빼고 **사건이 아니라 역량**을 말하는 페이지로 바꿨습니다.

### 01 담당 범위 — 30초 판단용

케이스 하나만 깊게 보여주면 *"오프라인 이벤트만 한 사람"*으로 읽힙니다. 이력서에 적힌 업무를 6칸 그리드로 먼저 펼치고, 각 칸에 **어느 브랜드에서 한 일인지**를 붙였습니다.

`검색 · SEO` / `퍼포먼스 광고` / `소재 · 콘텐츠` / `인플루언서 마케팅` / `오프라인 캠페인` / `언론 · 대행사`

### 02 기준 — 「흔히 보는 것 → 실제로 본 것」

| | 흔히 보는 것 | 실제로 본 것 |
|---|---|---|
| 광고 운영 | 노출 수 · 클릭 수 | **전환율로 소재와 예산을 정했습니다** |
| 인플루언서 | 팔로워 규모 | **브랜드 아이덴티티와 맞는지를 먼저 봤습니다** |
| 상세페이지 | 좋다고 쓰는 문장 | **근거가 될 자료를 함께 붙였습니다** |
| 대행사 | 맡기고 결과를 기다림 | **꾸준히 모니터링하고 요구를 상세하게 넘겼습니다** |

넷을 나열하면 "이것도 했다"가 되지만, 대비로 놓으면 **판단 기준**이 됩니다. 그리고 이건 회사가 바뀌어도 재현되는 부분이라 담당 범위 바로 뒤에 뒀습니다.

### 03 검색 — 하이이미지 (사이트에 처음 등장)

첫 직장 1년 2개월이 사이트에 통째로 비어 있었습니다.

> **전국에서 못 이기면, 동네에서 이기면 됩니다**
> 키워드 분석 → 지역 키워드 → 상담 문의
> *하루 8~12개씩 포스팅을 쓰면서도, 본 건 발행 수가 아니라 문의 수였습니다.*

마지막 줄이 페이지를 묶습니다 — *"이길 수 없는 자리에서 버티는 대신, 이길 수 있는 자리를 골랐습니다."*

### 04 만드는 비용 — 직접 + AI

```
[예전 · 시안 한 벌] → [지금 · AI로 초안] → [고르는 기준]
     한 방향              여러 안              반응
```

> 중요한 건 AI를 쓴다는 사실이 아니라 **만드는 비용이 내려간 만큼 고를 거리가 늘었다**는 점입니다. 어느 안을 남길지는 여전히 반응을 보고 정합니다.

"AI 잘 씁니다"는 변별력이 없어서, **판단할 대안이 늘었다**로 잡아 사이트 전체 논지와 이었습니다.

---

## 3. 케이스 상세 페이지 3개 제거

| 페이지 | 이유 | 링크 정리 |
|---|---|---|
| `bj-groupbuy.html` | 포트폴리오에서 제외 | 2곳 |
| `global-launch.html` | 제외 | 1곳 |
| `expo.html` | MARKETING 본문과 중복 | 1곳 |

전부 `_to_delete/`로 옮겼습니다(이 환경에선 삭제가 막혀 있음). 지울 때마다 앞뒤 케이스 링크를 다시 이어야 해서, **하단 이동 링크를 `← 목록으로 돌아가기` 하나로 통일**했습니다. 이제 케이스가 늘든 줄든 손댈 일이 없습니다.

`projects/`에는 `fragrance-crm.html` 하나만 남았습니다.

---

## 4. 잡은 버그

### CSS 규칙 누락 4건

| 위치 | 증상 |
|---|---|
| `.brandResult a` | 390px에서 `보/기`로 쪼개짐 |
| **`css/project.css` 전체** | keep-all이 1개뿐이라 상세 페이지 본문이 전부 글자 단위로 쪼개짐 (`매/장`, `노/트`, `카테/고리`) |
| `.osValue` | `중복 적/용`으로 쪼개짐 |
| `.projSection p + p` | 문단 여백이 없어 한 덩어리로 붙어 보임 |

### 사진 처리 사고 2건

**세로 사진을 가로 프레임에 넣어 잘림** — `sinsan-tasting.webp`(900×1200)과 `sinsan-ig-expo-wishlist-cover.webp`(1000×1250)를 `.aboutPhotoSlot.isShot`(310×174)에 넣어 위아래가 통째로 잘려 있었습니다. 원본 비율을 유지하는 `.kvFigure`로 교체.

**투명 배경이 검정으로 변환** — 대표 이미지 원본이 RGBA인데 그냥 RGB로 저장하면 카드 주변이 새까맣게 나옵니다. 흰색에 합성해 저장.

**전단 이미지의 이중 모서리** — PNG마다 1px 검정 외곽선이 구워져 있어 테두리가 겹쳤습니다. 외곽선을 잘라내고 테두리 대신 그림자로. (이후 전단 사진 자체를 도식으로 교체)

### 사이드 탭 오작동

`projects/` 상세 페이지에서 `.projHeader` · `.projIntro` · `.projMeta` · `.projBody` · `.projNav`를 섹션으로 세어 **의미 없는 `SECTION 1~5` 탭**이 붙고 있었습니다. `.projBody`가 있으면 사이드 탭을 아예 만들지 않도록 처리.

### 아이콘

`fa-magnifying-glass-chart` · `fa-chart-simple` · `fa-arrows-turn-right` · `fa-circle-nodes`가 **FA 6.0.0에 없어서** 다른 이름으로 대체했습니다. 예전 `fa-mobile-screen` 사고와 같은 계열이라, 쓰기 전에 패키지를 받아 전부 확인했습니다. CSS `::before`로 넣은 것(`\f002` · `\f0eb` · `\f058`)은 브라우저에서 실제 렌더된 코드포인트까지 재확인.

---

## 5. 상세 페이지 아이콘 체계

`projects/*.html`이 라벨 + 문단만 반복돼 밋밋했습니다.

- **섹션 라벨** — 원형 배지(`#FFF1EF` 원 + 코랄 아이콘). `problem`=돋보기 / `solve`=전구 / `result`=체크. CSS로 붙여서 라벨 이름이 `문제 발견`이든 `배경`이든 따라옴
- **메타 바** — 브랜드 · 주기 · 역할 · 채널에 아이콘
- **결과 스탯** — 코랄 원 안의 아이콘

---

## 파일 현황

| 파일 | 크기 | 비고 |
|---|---|---|
| `index.html` | 9,594 B | about.html 복사본 |
| `about.html` | 9,613 B | 03 섹션 차트 → 흐름도 + 링크 |
| `brand-story.html` | 28,355 B | 변경 없음 |
| `data-crm.html` | 25,466 B | **신설** 5섹션 |
| `marketing.html` | 15,889 B | **신설** 4섹션 |
| `resume.html` | 18,325 B | 변경 없음 |
| `404.html` | 1,763 B | |
| `projects/fragrance-crm.html` | — | 대표 이미지 · 내용 갱신 |
| `css/site.css` | — | 도식 8종 추가 |
| `css/project.css` | — | keep-all · 아이콘 · KV 프레임 |
| `css/rwd.css` | — | 새 블록 모바일 규칙 |
| `js/site.js` | — | 문서형 분기 · 상세 페이지 탭 제거 |

**새 이미지** — `merdescent-fragrance-kv.webp`(566×848)

---

## 검증 방식

모든 변경은 실제 브라우저(Chromium)로 렌더링해 확인했습니다.

- **8개 페이지 × PC(1280) · 모바일(390)** — 가로 넘침 0건 · JS 에러 0건 · 빈 아이콘 0건
- **링크 전수 검사** — 깨진 링크 0건. HTML이 참조하는 이미지 전부 존재 확인
- **줄바꿈 실측** — 글자 단위로 좌표를 재어 어절 중간 분리 0건
- 아이콘은 **FA 6.0.0 패키지를 받아 존재 여부와 유니코드를 직접 확인**
- 검사 스크립트도 두 번 고쳤습니다 — `<small>`이 섞이면 줄바꿈을 오탐하던 것(top 비교 → 세로 겹침 비교), 숨긴 아이콘을 빈칸으로 세던 것

> **한 가지 한계** — 이 환경에서 런드리고딕 웹폰트 CDN(`cdn.jsdelivr.net`)이 403으로 막혀 Noto Sans CJK로 대체 측정됐습니다. 한글이 실제 폰트보다 넓은 쪽이라 줄 수는 안전한 방향이지만, 실제 브라우저에서 한 번 확인해보시는 게 확실합니다.

---

## 남은 일

| 항목 | 내용 |
|---|---|
| **⚠️ 할인율 불일치** | 매장 전단은 `UP TO ~15% OFF`, 대표 이미지는 `UP TO 20% OFF`. 지금은 "달에 따라 최대 20%까지"로 적어 두고 HTML에 `▼ 확인 필요` 주석을 남김 |
| **⚠️ `7~8%` 라벨** | BRANDING은 `신규 유입 팔로워 실구매 전환율`, ABOUT·홈은 `신규 고객 재구매 전환율`. 같은 수치인지 확인 필요 |
| **이력서 PDF** | `6개월 운영` 두 곳이 남아 있음. 사이트는 `매달 · 상시`로 통일됨 |
| **이력서 `0 → 1`** | 핵심 성과 중 하나인데 글로벌 런칭 상세 페이지를 지워서, 지금은 BRANDING 안쪽과 MARKETING 04 카드로만 연결됨 |
| **`index.html`** | 여전히 about.html 복사본. ABOUT 본문을 고칠 때마다 같이 반영해야 함 |
| **`_to_delete/`** | 통째로 삭제 가능. `.gitignore`에 넣어두는 것도 좋음 |
| **깃허브 업로드** | 이번 변경분 전체. `_to_delete/`는 빼고 |
| **미사용 이미지** | `merdescent-pop-*.webp` 3장(전단), `sinsan-tasting.webp` 등 |
| **MARKETING 보강** | 팝업스토어 · 전시회, 인플루언서 협업 상세는 아직 이름만 있음 |
