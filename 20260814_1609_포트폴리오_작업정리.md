# 포트폴리오 작업 정리

> 심민용 포트폴리오 (`minyongsim/portfolio_sim2026`)
> 정리 시점: 2026년 8월 14일 16:09 KST

---

## 이번 작업의 큰 줄기

첫 화면 메인 카피를 바꾸고, ABOUT과 BRAND STORAGE 두 페이지의 레이아웃·비율·모션을 정리했습니다. 그 과정에서 발견한 **레이아웃 버그 세 가지**도 같이 잡았습니다.

가장 중요한 변화는 **메인 카피 교체**입니다. 기존 "데이터로 마케팅하는 심민용입니다"는 뒤에 나오는 내용(브랜드 정체성, 패키지 리뉴얼, 박람회, 글로벌 사이트 구축)과 어긋나 설득력을 떨어뜨리고 있었습니다.

---

## 1. 첫 화면 메인 카피 교체

### 무엇이 문제였나

첫 화면이 **네 군데에서 동시에 퍼포먼스 마케터 신호**를 보내고 있었습니다.

- 헤드라인 "**데이터로** 마케팅하는"
- 돋보기 아이콘
- 배경 워터마크 `DATA`
- 태그 `DATA-DRIVEN` / `CRM · GROWTH`

그런데 실제로 이어지는 내용은 브랜드 정체성 정립, 패키지 리뉴얼, 박람회 현장 기획, 글로벌 사이트 구축, 인플루언서 공동구매입니다. 첫 화면에서 기대하게 만든 것과 실제 내용이 달라 설득력이 떨어지는 구조였습니다.

### 어떻게 바꿨나

| 항목 | 이전 | 지금 |
|---|---|---|
| 헤드라인 | 데이터로 마케팅하는 심민용입니다 (52px) | **만들고, 팔고, 확인합니다** (62px) |
| 소개 문장 | 숫자가 안 맞으면, 다시 봅니다. | **정체성을 만들고, 온·오프라인에서 팔고, 데이터로 확인했습니다.** |
| 머리표 | `01` | **SIM MIN YONG** |
| 아이콘 | 돋보기 | 레이어 |
| 워터마크 | DATA | **WORKS** |
| 태그 | MARKETER / DATA-DRIVEN / CRM·GROWTH | **커피 · 화장품 / 국내 · 해외 / 기획 → 운영** |

**헤드라인의 세 동사가 상단 메뉴와 그대로 맞물립니다.**
만들고 = BRAND STORAGE · 팔고 = MARKETING · 확인합니다 = DATA & CRM

역할도 나눴습니다 — 헤드라인은 **일하는 순서**, 소개 문장은 **실제 한 일**, 태그는 **범위**. 세 줄이 각자 다른 정보를 담아 겹치지 않습니다.

### 곁들여 처리한 것

- 헤드라인에서 이름이 빠져 머리표 자리에 `SIM MIN YONG`을 넣음
- 글자 크기를 68px로 잡았더니 콘텐츠 기둥(832px)을 정확히 꽉 채워 웹폰트가 조금만 넓어도 두 줄로 접힐 상태 → **62px**로 낮춰 20% 여유 확보
- 모바일 워터마크 110 → 88px (`WORKS`가 5글자라 화면 밖으로 잘림: 452px > 390px)

---

## 2. 레이아웃 버그 세 가지

### (1) 콘텐츠 기둥 폭이 섹션마다 제각각

`.aboutSectionInner`에 폭 제한이 없어 **"그 섹션에서 가장 긴 한 줄"에 맞춰 늘어나고** 있었습니다.

- 실측: **649 / 765 / 844 / 1098px** (2.8배 차이)
- 스크롤할 때 좌우로 출렁이는 원인
- 3번 섹션은 헤드라인이 1050px인데 차트는 620px — **머리가 몸통보다 1.7배 넓은** 상태

**조치** — 기둥을 880px로 통일하고, 헤드라인에 상한(820px)을 둬 긴 것만 두 줄로 접히게 함.

| 3번 섹션 | 이전 | 지금 |
|---|---|---|
| 머리 (헤드라인) | 1050px | **564px** |
| 몸통 (차트) | 620px | **780px** |
| 맺음 문장 | 805px | **411px** |

BRAND STORAGE도 같은 문제가 있어(헤드라인 최대 818px vs 콘텐츠 852px) 소개 문장과 같은 700px 기둥에 맞췄습니다.

### (2) 스냅 후 섹션 머리가 헤더에 가려짐

원인이 두 개였습니다.

- **띠배너+상단바가 `position: sticky`** → 스크롤 중 상단바가 111 → 90px로 줄면 문서 흐름 높이까지 21px 줄어 목표 지점이 어긋남
- **등장 애니메이션(`.reveal`)이 `translateY(18px)`을 걸어둠** → `offset()`이 실제보다 18px 아래로 나오고, 도착하면 애니메이션이 풀리며 그만큼 위로 올라감

**조치**
- 상단 고정 영역을 `fixed`로 빼고, 같은 높이의 빈 자리(`.chromeSpacer`)를 JS가 생성
- transform 영향을 안 받는 `offsetTop` 누적으로 위치를 재는 `docTop()` 함수 도입

| 섹션 상단 위치 | 이전 | 지금 |
|---|---|---|
| 섹션 2 | 72px (52px 가려짐) | **124px (0px)** |
| 섹션 3 | 93px (31px 가려짐) | **124px (0px)** |
| 섹션 4 | 106px (18px 가려짐) | **124px (0px)** |

헤더 바닥이 정확히 124px이므로 딱 맞습니다.

### (3) 섹션이 화면 밖으로 밀려남

섹션이 `100vh`인데 그 위를 띠배너+상단바 145px가 덮고 있어, 가운데 정렬한 내용이 **실제 보이는 영역 기준으로 74px 아래**에 앉아 있었습니다. `calc(100vh - var(--chromeH))`로 바꿔 **11px**로 줄였습니다.

---

## 3. ABOUT 페이지

### 첫 화면 (INTRO)

- **워터마크** 정중앙 → 섹션 위쪽으로 이동 + 아래쪽 페이드 마스크 (280px `DATA`가 헤드라인을 관통하고 있었음)
- **세로 리듬** 24 / 18 / 22 (거의 균등) → **14 / 30 / 36** — [머리표+헤드라인] / [소개] / [태그] 세 덩어리로 읽히게
- **코랄 반복 정리** — 세로로 쌓인 코랄 아이콘 + 코랄 번호를 `[아이콘] ─ 01` 가로 한 줄로 묶고 번호를 회색으로 낮춤
- **태그 위계** — 1.5px 진한 코랄 테두리가 소개 문장보다 먼저 눈에 들어와 순서가 뒤집혀 있었음 → 1px 연한 톤
- **스크롤 유도** `SCROLL` + 코랄 조각이 내려가는 선 (40px만 내려도 사라짐)
- **점선 제거** — `2px dashed`는 스크롤 테스트용 눈금이었는데 상단바 밑 개발용 선처럼 보였음

### 첫 화면 모션

문장이 세 박자라 그대로 모션으로 옮겼습니다.

- **만들고, → 팔고, → 확인합니다**가 하나씩 톡톡 등장. 착지할 때 **약 2px 지나쳤다 돌아오는** 오버슈트 곡선 (실측 20px → 0 → −1.95px → 0)
- **헤드라인에 마우스를 올리면** 같은 순서로 0.08초씩 밀려 한 번 더 통통 튐
- 태그 3개도 하나씩 살짝 커지며 등장

전체 흐름: `머리표(0.1s) → 만들고(0.18) → 팔고(0.29) → 확인합니다(0.40) → 소개(0.52) → 태그(0.62/0.70/0.78)` — 1.35초 마무리

> **함정 하나** — 처음엔 시차가 전혀 재생되지 않았습니다. 숨김 상태(`.reveal`)에 transition을 걸어둔 게 원인이었는데, 첫 화면은 이미 보이는 상태라 숨김과 등장 클래스가 40ms 안에 연달아 붙으면서 "1에서 0으로 가는 중"에 곧장 되돌아가 딜레이가 무의미해졌습니다. **숨김은 즉시, 등장할 때만 transition**을 거는 구조로 바꿔 해결했습니다.

### 3번 섹션 — 쏠림에서 분산으로 (차트 신규)

이 섹션은 주장만 있고 증거가 없었습니다.

- 화면의 **37%만 채움**
- 헤드라인과 소개 문장이 **같은 말**을 두 번
- 큰 숫자가 성과가 아니라 **소요 기간(6개월)**

**덤벨 차트를 새로 만들었습니다.** 데이터가 하는 일이 "항목별 before → after"라 덤벨을 골랐습니다.

| 항목 | 변화 |
|---|---|
| 룸스프레이 | 유지 |
| 디퓨저 | 크게 상승 |
| 그 외 향 노트 | 상승 |

- **수치는 한 개도 표기하지 않았습니다.** 방향과 상대 위치만 표시하고 *"실제 수치는 계약상 공개할 수 없어, 방향과 상대적 위치만 표시했습니다"*를 명시 — NDA를 지킬 줄 안다는 신호가 됩니다
- 값 자리에는 **"유지" / "크게 상승" / "상승"**을 글자로 직접 표시 → 색을 못 봐도 전달됨
- 색은 **강조(emphasis)** 방식: 이전 = 무채색 회색(맥락), 이후 = 브랜드 코랄(핵심)
- 검증기 실행: 색각 이상 구분도 **ΔE 11.3(deutan)**, 일반 시야 **20.1** — 기준(8 / 15) 통과
- 큰 숫자 "6개월"은 빼되 사실은 살림 (범례를 "이전 / 6개월 후"로)
- 중복 카피 정리 → *"베스트셀러 한 곳에 몰려 있던 관심을, 6개월간의 향 노트 캠페인으로 다른 카테고리와 향 노트까지 넓혔습니다."*

화면 채움률 **37% → 56%** (모바일 81%)

### 4번 섹션 버튼

내용은 그대로 두고 디자인만 손봤습니다.

- 2px 검정 테두리 + hover 시 통째로 검게 반전 → **1.5px 연한 회색 + hover 코랄** (사이트의 나머지는 얇은 선 + 코랄 포인트인데 여기만 무거웠음)
- **유령 여백 제거** — `padding-bottom: 60px`이 눈에 안 보이는데 블록 높이에는 들어가 내용이 위로 치우쳐 보였음
- 버튼 45 → **55px**, 최소 폭 240px
- **카테고리 아이콘 추가** — 폴더(BRAND STORAGE) · 파이차트(DATA & CRM) · 확성기(MARKETING)
- `→` 화살표 어포던스 (hover 시 오른쪽으로 밀림)
- 요소별 시차 등장 (머리표 → 헤드라인 → 소개 → 버튼 1·2·3, 약 940ms)
- 모바일 버튼 폭 220/200/200 제각각 → **280px 통일**

---

## 4. BRAND STORAGE 페이지

여덟 섹션이 전부 같은 구조로 흘러 단조로웠습니다.

### 사진

직접 만든 패키지가 **160×120px 우표 크기**로 12장 똑같이 깔려 있었습니다.

- **260×195px**로 확대 (면적 2.6배)
- 사진 묶음만 글 기둥(852px)을 넘어 **1120px로 펼쳐지게** → 글은 좁고 사진은 넓은 리듬
- 간격 34 → 16px (사진을 키우니 한 줄에 4장이 안 들어감: 4장 1040 + 간격 102 = 1142px)
- 모바일 120×90 → **158×119** (한 줄 2장), 사이트 캡처는 글자가 있어 **300×169** 한 줄 한 장

### 구조

- **브랜드 라벨 여덟 번 반복** → `01 ─ 브랜드 정체성` 머리표로 교체. 브랜드 표시는 탭이 담당
- **사진 없는 섹션의 흐름도를 카드형으로 승격** — 메르디센트 기획(412px)·결과는 시각 요소가 하나도 없었음. 같은 페이지의 주문 흐름 카드와 같은 형태로 올림 (`:has()` 사용, 미지원 브라우저는 기존 모습 유지)
- **점선 구분선** `2px dashed` → `1px solid`
- **브랜드 탭에 로고** — 신산커피 로고 + 메르디센트 워드마크

### 브랜드 간 상호 링크 (신규)

두 브랜드가 서로를 **한 번도 안 가리키고** 있었습니다. 신산커피를 끝까지 읽으면 탭까지 **4,596px을 거슬러 올라가야** 메르디센트로 갈 수 있어, 그런 게 있는 줄 모르고 떠날 수 있는 구조였습니다.

- 신산커피 04 결과 → `메르디센트 글로벌 보기` (코랄 강조)
- 메르디센트 04 결과 → `신산커피 보기`
- 각각 상단 탭과 **같은 마크** 사용
- 브랜드 전환 시 **맨 위로 0.5초에 걸쳐 이동** — 안 하면 4,000px 넘게 내려온 자리에서 탭만 바뀌어 새 브랜드의 엉뚱한 중간을 보게 됨
- 주소창 해시(`#merdescent` / `#sinsan`)도 함께 변경 → 그 상태로 링크 공유 가능

### 메르디센트 로고 확보

로고 파일이 없어서, 이미 갖고 있던 **`merdescent-site-about1.webp`(사이트 ABOUT 캡처) 헤더에서 추출**했습니다.

- 메인 페이지 캡처는 사진 위 흰 글자라 사용 불가 → ABOUT 캡처는 배경이 거의 흰색이라 분리 가능
- 어두운 픽셀 영역을 찾아 좌표를 잡고(102×14px), 밝기값으로 알파 채널을 만들어 배경을 투명 처리
- 원본 글자색 `RGB(45,45,44)` — 사이트 기본색 `#373737`과 거의 동일
- 3배 확대 + 가장자리 대비 복원 → **311×46px**, `img/merdescent-logo.png`

> 원본이 캡처 안의 102×14px이라 지금처럼 12px 높이로 작게 쓸 땐 문제없지만, 크게 쓸 일이 생기면 원본 로고 파일이 필요합니다.

---

## 5. 그 밖에

### 사진 27장 복구

폴더의 `brand-story.html`이 **예전 버전으로 되돌아가 있었습니다**(22,784B). 그 파일은 이미지를 `.jpg`로 찾는데 `img/` 안에는 전부 `.webp`로 바뀌어 있어 신산커피 사진 **27장이 통째로 깨져** 있었습니다. OG 메타, 메르디센트 스크린샷 6장, 주문 흐름 5단계도 함께 빠져 있었습니다. 최신본으로 복구했습니다.

### 휠 스냅

- **BRAND STORAGE에서는 제거** — 섹션 높이가 제각각이라 스냅이 오히려 어색했음
- **ABOUT은 감도 상향** — 한 번 굴리면 800ms 동안 다음 입력을 무시하던 것을 **510ms**로 (애니메이션 700 → 450ms, 여유 100 → 60ms). 실측으로 600ms 간격 연속 스크롤 시 이전 3칸 → **5칸(끝까지)**. 트랙패드 관성으로 두 칸 넘어가는 부작용은 없음

### 푸터 리디자인

- 워터마크 `THANKS`를 바닥에 붙여 잘리게 하고, 윗부분이 사라지는 마스크 적용
- 왼쪽 로고+맺음말 / 오른쪽 연락처 **2단 배치**
- 사이트 로고 추가 (흰색 반전)
- RESUME 아이콘이 지구본이었는데 PDF 링크라 `fa-file-pdf`로 교체
- `{{BASE}}` 치환 추가 (하위 폴더 페이지 대비)

### 줄바꿈 정리

헤드라인이 두 줄로 접히며 어절이 쪼개지고 있었습니다.

- ~~기획부터 결제까지, **전** / **과정**을~~ → 기획부터 결제까지, / 전 과정을
- ~~꾸준한 해외 문의에, **제대로** / **응답할**~~ → 꾸준한 해외 문의에, 제대로 응답할 /
- ~~쏠려 있던 매출을, **골고루** / **팔리게**~~ → 쏠려 있던 매출을, / 골고루 팔리게

붙어야 할 어절을 `&nbsp;`로 묶어 전부 쉼표 자리에서 끊기게 했습니다.

### 검토했지만 반영하지 않은 것

- **상단바 재배치** — 로고 왼쪽/메뉴 오른쪽, 분할 메뉴 등 7가지 안을 미리보기로 검토 후 **원안 유지**
- **4번 섹션 케이스 카드** — 버튼을 케이스 카드로 바꿨다가 **원래 버튼으로 되돌림**

---

## 남은 일

| 항목 | 내용 |
|---|---|
| **깃허브 업로드** | `img/` 폴더를 반드시 함께 올릴 것 (확장자가 `.webp`로 바뀐 파일 다수). `404.html`, `img/og-cover.jpg`, `img/merdescent-logo.png`도 새 파일 |
| **빈 페이지** | `data-crm.html` · `marketing.html`이 아직 "SECTION 1~4"만 있는 상태. 4번 섹션 버튼이 이리로 연결됨 |
| **메르디센트 로고 원본** | 캡처에서 추출한 것이라 작게만 사용 가능. 원본 파일이 있으면 같은 이름으로 덮어쓰기 |
| **패키지 리뉴얼 섹션** | 과거에 유실된 섹션. `sinsan-card-*.webp` 4장이 아직 미사용 |
| **8% 문구 통일** | ABOUT은 "신규 고객 재구매 전환율", BRAND STORAGE는 "신규 유입 팔로워 실구매 전환율" |
| **워터마크 중복** | 3번 섹션 배경이 `GROWTH`, 태그에도 관련 단어가 있는지 확인 필요 |

---

## 파일 현황

| 파일 | 크기 |
|---|---|
| `index.html` | 9,595 B |
| `about.html` | 9,614 B |
| `brand-story.html` | 28,709 B |
| `data-crm.html` | 2,675 B (미완성) |
| `marketing.html` | 2,653 B (미완성) |
| `404.html` | 1,768 B |
| `css/site.css` | 50,183 B |
| `css/style.css` | 17,536 B |
| `css/rwd.css` | 7,983 B |
| `js/site.js` | 34,439 B |
| `js/partials.js` | 2,500 B |

---

## 검증 방식

모든 변경은 실제 브라우저(Chromium)로 렌더링해 확인했습니다.

- **6개 페이지 × PC(1280px) / 모바일(390px)** — JS 에러 0건
- 요소 좌표·크기·색상을 픽셀 단위로 실측 (헤더 바닥 = 스페이서 높이 = 첫 섹션 위치 일치 확인)
- 애니메이션은 **프레임 단위로 opacity·transform 값을 기록**해 순서와 오버슈트 확인
- 색상은 검증 스크립트로 색각 이상 구분도 계산
- 모션 최소화(`prefers-reduced-motion`) 설정에서도 콘텐츠가 정상 표시되는지 확인
- 가로 스크롤 넘침, 깨진 이미지 개수 확인
